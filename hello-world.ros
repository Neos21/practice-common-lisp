#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.hello-world.3872504799
  (:use :cl))
(in-package :ros.script.hello-world.3872504799)

(defun main (&rest argv)
  (declare (ignorable argv))
  ; ココにメイン処理を書いていく
  
  (write-line "Hello world!")
  
  0  ; 終了ステータス
)
;;; vim: set ft=lisp lisp:
